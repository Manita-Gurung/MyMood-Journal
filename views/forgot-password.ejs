<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forgot Password - MyMood Journal</title>
    <link rel="stylesheet" href="/css/auth.css">
</head>
<body>
    <div class="auth-container">
        <a href="/users/login" class="back-button">← Back</a>
        <div class="auth-box">
            <h2>Forgot your password?</h2>
            <p class="auth-description">Enter your email address to receive verification code</p>
            
            <!-- Error Messages -->
            <% if(typeof error_msg !== 'undefined' && error_msg.length > 0) { %>
                <div class="alert alert-danger">
                    <%= error_msg %>
                </div>
            <% } %>
            
            <!-- Success Messages -->
            <% if(typeof success_msg !== 'undefined' && success_msg.length > 0) { %>
                <div class="alert alert-success">
                    <%= success_msg %>
                </div>
            <% } %>
            
            <form action="/users/forgot-password" method="POST">
                <div class="form-group">
                    <label for="email">Email address</label>
                    <input type="email" id="email" name="email" required>
                </div>
                <button type="submit" class="submit-button">Send code</button>
            </form>
        </div>
    </div>
</body>
</html> 